{% extends 'base.html.twig' %}

{% form_theme form_type 'bootstrap_4_layout.html.twig' %}

{% block title %}Classes{% endblock %}

{% block body %}
    <div class="jumbotron">
        <h1>{% if id %}Editer la{% else %}Nouvelle{% endif %} classe</h1>
    </div>
    <div class="container">
        <div class="col-xs-12 col-lg-4 col-md-4">
            {{ form_start(form_type) }}
            {{ form_row(form_type.nom) }}
            <button type="submit" class="btn btn-success">{% if id %}Editer{% else %}Créer{% endif %}</button>
            {{ form_end(form_type) }}
            <a href="{{ path('admin_list_classes') }}"><button class="btn btn-primary">Retour</button></a>
        </div>
    </div>
{% endblock %}