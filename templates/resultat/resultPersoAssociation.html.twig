<div class="container">
    <div class="row">
        <div class="col-12 col-lg-9 col-md-11">
            <p>Pour l'association ce sont les mauvaises réponses</p>
            {% for response in responseEleveList %}
                <ul>
                    <li>{{ response.reponse }} => {{ response.groupe }}</li>
                </ul>
            {% endfor %}
        </div>
    </div>
</div>