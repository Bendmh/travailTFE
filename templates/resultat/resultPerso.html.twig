{#{% extends 'base.html.twig' %}

{% block title %}Resultats{% endblock %}

{% block body %}
    <div class="jumbotron">
        <h1>Résultat de {{ user.prenom }} {{ user.nom }} pour l'activité : {{ activity.name }}</h1>
    </div>#}
    <div class="container">
        <div class="row">
            <div class="col-12 col-lg-9 col-md-11">

                {% set previousResponse = null %}
                {% for response in responseEleveList %}
                    {% if previousResponse is null or previousResponse.questionId.id != response.questionId.id %}
                        <h5>{{ response.questionId.question }}</h5>
                     {% endif %}
                    <ul>
                        <li>{{ response.reponse }}</li>
                    </ul>
                    {% set previousResponse = response %}
                {% endfor %}
            </div>
        </div>
    </div>
{#
{% endblock %}#}
